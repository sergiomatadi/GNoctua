
/*package gnoctua.Modelo;
            
import java.util.List;

public class ClienteDAOMemory extends ClienteDAO{
    
    private static Cliente[] clienteArray =
            new Cliente[20];

    /**
     *
     * @param nif
     * @return
     */
/*    @Override
    public boolean delete(String nif) {
        clienteArray][nif] = null;
    }

    @Override
    public boolean update(Cliente a) {
        clienteArray][client.getNif()] = client;
    }

    @Override
    public Cliente read(String email) {
        clienteArray][client.getNif()] = client;
    }

    @Override
    public boolean create(Cliente a) {
        clienteArray][client.getNif()] = client;
    }
    
    @Override
    public Cliente findByEmail(String nif){
        return clienteArray[nif];
    }
    
    @Override
    public Cliente[] getAllClientes(){
        List<Cliente> emps = newArrayList<>();
        for (Cliente e: clienteArray){
            if(e!=null){
                client.add(e);
            }
        }
        return clientArray(new Cliente[0]);
    }   
}*/
   